# Core deps for the mrrecon toolkit (torch already installed: 2.8.0+cu129).
torch>=2.1
numpy>=1.24
h5py>=3.8
scikit-image>=0.21
matplotlib>=3.7
tqdm>=4.65
monai>=1.5          # supervised backbones: UNet, SwinUNETR
einops>=0.7         # required by SwinUNETR
